%table.table.table-responsive

  - @operas.each do |p|
    %tr
      %td
        = link_to(image_tag(p.image.url(:quad2), class:'img-responsive'), p)
      %td
        Dimensioni
        - if p.size.present?
          %em= p.size
        - else
          %em n/a
        %br
        Offerta attuale 
        - if p.price.present?
          %strong= p.price
        - else
          %em Da stabilire
        %br
        = link_to 'Dettagli', p

